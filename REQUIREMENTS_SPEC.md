# Anritsu STD Scenario Viewer - Requirements Specification (요구사항 명세서)

> **[중요] 본 문서는 안리츠 시나리오 뷰어의 GUI 레이아웃 및 렌더링 아키텍처 원칙을 정의한 영구 명세서입니다. 추후 수정 및 기능 확장 시 본 명세서의 규칙을 반드시 100% 준수해야 합니다.**

---

## 📌 1. 메인창 레이아웃 명세 (Main Window Layout Specification)

- **[원칙 1] 연결 맵 기반 트리·레벨 배치**
  - 메인 창 (Scope: `root`)도 모든 outcome을 먼저 연결 맵으로 구성한 뒤, 결정적 트리와 위상 레벨을 기준으로 배치한다.
  - 주 흐름의 부모·자식은 가능한 한 동일 열의 연속 레벨에 배치해 박스 하단에서 다음 박스 상단으로 곧게 내려가는 수직 직진선을 유지한다.
  - 두 번째 열 분기는 반드시 출발 박스보다 **아래 레벨**에서 시작한다. 같은 행 또는 위쪽으로 되돌아가는 열 간 연결은 허용하지 않는다.
  - 중간 박스 안전 여백이 비어 있는 같은 열 하향 연결은 결과 유형과 관계없이 직선으로 렌더링한다.

---

## 📌 2. 하단 서브창 레이아웃 명세 (Child Window Layout Specification)

- **[원칙 2-1] 정상 루틴 하향 직진 (Normal Routine Flow)**
  - 정상 동작 시퀀스 노드 (`START`, `RRC Connection...`, `Attach Request`, `PDN Connectivity Request`, `Conditional Branch` 메인 경로, `Authentication`, `NAS_Security`, `Create_Attach_Accept_Message`, `Attach Complete`, `Activate Default EPS Bearer Context Accept`, `CompoundTerminator` 등)는 모두 **좌측 메인 1열 (`X = 100px`) 축을 따라 정직하게 아래로 흐르는 수직 직진 구조**를 유지해야 합니다.
  - 정상 전이 결과 정의: `OK`, `Assigned`, `Logged`, `Displayed`, `True`, `Response Received`, `""` (빈값).

- **[원칙 2-2] 비정상 / 이상 루틴 물리적 우측 배치 (Exception Routine Layout)**
  - 오직 `Timeout`, `False`, `Failed`, `Fail`, `Error` 등의 이상/예외 결과를 받는 전용 타깃 노드만 **우측 신규 컬럼 (`Right Column, X = 500px`)으로 물리적으로 이동시켜 배치**합니다.

---

## 📌 3. 연결선 및 텍스트 겹침 제로화 명세 (Zero Line & Text Overlap Specification)

- **[원칙 3-1] 직통 수평 브릿지선 (Direct Horizontal Bridge Line)**
  - 좌측 정상 노드(`X = 100px`)에서 우측 예외 노드(`X = 500px`)로 연결되는 분기선은 사이에 수직 버스 트랙을 거치지 않고 **노드 오른쪽 끝(`X = 370px`)에서 우측 예외 노드 왼쪽 입구(`X = 500px`)로 직접 뻗어나가는 직통 수평 브릿지선 (`▶` 방향 화살표)**으로 연결합니다.
  - 이 구간(`X = 370px ~ 500px`)에는 세로 버스선이 아예 지나가지 않으므로 **정상 루틴과 비정상 루틴의 연결선이 기하학적으로 겹칠 가능성이 100% 물리적으로 차단**됩니다.

- **[원칙 3-2] 외곽 수직 버스 트랙 분리 (Outer Bus Track Separation)**
  - 예외 노드에서 출발하여 종결 노드로 빠져나가는 수직 버스 라인들은 우측 예외 컬럼 뒤편 (`X >= 800px`) 영역에만 존재하며, **`25px` 레인 간격 (`X = 800px, 825px, 850px...`)으로 고유 트랙 오프셋**을 부여하여 선 끼리 포개짐이 0%여야 합니다.

- **[원칙 3-3] 텍스트 라벨 분리 배치 (Outcome Label Placement)**
  - `True` / `OK` 등 정상 라벨: 수직 직진선 우측에 배치 (초록색 font).
  - `False` / `Timeout` / `Failed` 등 예외 라벨: 수평 이탈 마디 위 중앙에 배치 (빨간색 font).
  - 라벨은 연결선 위가 아닌 *선 옆 또는 선 위*에 배치하여 글자-선 충돌 방지.

---

## 📌 4. 직교 라우팅 원칙 (Orthogonal Routing Specification)

> **모든 연결선은 반드시 수평선과 수직선만으로 구성된 직교(Orthogonal) 경로로 그려야 합니다. 대각선(Diagonal) 연결선은 절대 허용하지 않습니다.**

### 연결선 종류별 라우팅 규칙

| 케이스 | 연결 유형 | 색상 | 경로 |
|---|---|---|---|
| **A** | Normal → Normal (정상 직진) | 🟢 초록 | `src_bottom_centre` → `dst_top_centre` 수직 |
| **B** | Normal → Exception (이상 분기) | 🔴 빨강 | `src_right` → `bus_x`(수평) → `dst_Y`(수직) → `dst_left`(수평) |
| **C** | Right-col → Right-col, 정상 라벨 | 🟢 초록 | 수직 직진 (예: True: Conditional Branch → LogMessage) |
| **D** | Exception → Normal 복귀, 기타 | 🔵 파랑 | `src_right` → `bus_x`(수평) → `dst_Y`(수직) → `dst_right`(수평) |

### 수직 버스 트랙 겹침 방지 규칙

- **B형 (L→R 브릿지) 버스 트랙**: 첫 번째 트랙 X = `495px`, 각 연결선마다 `-20px` 이동 (495 → 475 → 455 → ...).
- **D형 (복귀) 버스 트랙**: 첫 번째 트랙 X = `820px`, 각 연결선마다 `+26px` 이동 (820 → 846 → 872 → ...).
- **각 연결선은 반드시 고유한 수직 트랙 X 좌표를 사용** → 두 선의 수직 구간이 절대 겹치지 않음.

### 핵심 불변 원칙
- 어떠한 두 연결선의 수직 세그먼트도 동일한 X 좌표를 공유해서는 안 된다.
- 어떠한 연결선도 대각선 구간을 포함해서는 안 된다.
- 라벨 텍스트는 출발 수평 구간 위(Y-13px)에 배치한다.

---

## 📌 5. 연결선 색상 명세 (Connector Color Specification)

| 결과 유형 | 색상 | RGB |
|---|---|---|
| 정상 (`OK`, `True`, `Assigned`, `Logged`, ...) | 🟢 초록 | `(40, 150, 40)` |
| 이상 (`Timeout`) | 🔴 빨강 | `(210, 30, 30)` |
| 이상 (`False`, `Fail`, `Failed`, `Error`) | 🔴 빨강 | `(210, 30, 30)` |
| 복귀 연결선 (Exception→Normal return) | 🔵 파랑 | `(20, 80, 200)` |

> **Timeout과 False/Fail은 동일하게 빨간색**으로 표시한다. (이전 주황색 Timeout 사용 금지)


---

## 📌 4. 직교 라우팅 원칙 (Orthogonal Routing Specification)

> **모든 연결선은 반드시 수평선과 수직선만으로 구성된 직교(Orthogonal) 경로로 그려야 합니다. 대각선(Diagonal) 연결선은 절대 허용하지 않습니다.**

### 연결선 종류별 라우팅 규칙

| 연결 유형 | 색상 | 경로 (3-segment) |
|---|---|---|
| **A. Normal → Normal** (정상 직진) | 🟢 초록 | `src_bottom_centre` → `dst_top_centre` 수직 |
| **B. Normal → Exception** (이상 분기) | 🟠 주황/🔴 빨강 | `src_right` → `bus_x`(수평) → `dst_Y`(수직) → `dst_left`(수평) |
| **C. Exception → Normal** (복귀) | 🔵 파랑 | `src_right` → `bus_x`(수평) → `dst_Y`(수직) → `dst_right`(수평) |

### 수직 버스 트랙 겹침 방지 규칙

- **B형 (L→R 브릿지) 버스 트랙**:
  - 첫 번째 트랙 X = `490px`, 각 연결선마다 `-20px` 씩 좌측 이동 (490 → 470 → 450 → ...).
  - 각 연결선은 **반드시 고유한 X 좌표**의 수직 세그먼트를 가지므로, 두 연결선의 수직 구간이 절대 겹치지 않음.

- **C형 (복귀) 버스 트랙**:
  - 첫 번째 트랙 X = `820px`, 각 연결선마다 `+26px` 씩 우측 이동 (820 → 846 → 872 → ...).
  - 역시 각 연결선이 고유한 X 좌표의 수직 세그먼트를 가짐.

### 핵심 불변 원칙 (Invariants)
- 어떠한 두 연결선의 수직 세그먼트도 동일한 X 좌표를 공유해서는 안 된다.
- 어떠한 연결선도 대각선 구간을 포함해서는 안 된다.
- 라벨 텍스트는 항상 연결선 옆 또는 위에 배치하고, 라인 위에 직접 겹쳐서는 안 된다.

---

## 📌 6. 예외 컬럼 분류 우선순위 규칙 (Exception Column Assignment Priority)

> **`True`/`OK` 등 정상 라벨로 도달 가능한 노드는 반드시 좌측 컬럼(정상 루틴)을 유지해야 한다.**

### 분류 원칙

| 노드 incoming 상태 | 컬럼 배치 |
|---|---|
| 정상 라벨로만 도달 가능 (`True`, `OK`, ...) | 🟩 **좌측 컬럼 (X=100)** |
| 정상 라벨 + 예외 라벨 혼합 도달 가능 | 🟩 **좌측 컬럼 (X=100)** — 정상 우선 |
| 예외 라벨로만 도달 가능 (`Timeout`, `False`, ...) | 🟥 **우측 컬럼 (X=520)** |

### 구현 규칙
- 노드 분류 1차 패스: 모든 outcome을 스캔하여 `normal_targets` / `excep_targets` 두 집합을 구성.
- `exc_set`(우측 컬럼 배치 대상) = `excep_targets - normal_targets`
  - 즉, **예외 라벨 incoming이 있더라도 정상 라벨 incoming이 하나라도 있으면 좌측 컬럼으로 분류**.
- 이 규칙 덕분에 `Conditional Branch`의 `True` 경로 목적지는 항상 좌측 컬럼에 배치되어 **`True` → 직진 하향 (녹색 직선)** 으로 렌더링된다.

---

## 📌 7. 라벨 텍스트 비겹침 규칙 (Label Non-Overlap Placement)

> **모든 라벨 텍스트는 연결선 픽셀 위에 직접 배치하지 않는다.**

| 연결선 유형 | 라벨 위치 |
|---|---|
| 수직 직진선 (A: Normal→Normal) | 선의 **우측 +14px**, Y는 선 중간 |
| L→R 브릿지 (B: Normal→Exception) | 출발 수평 구간 **위 -13px**, X는 출구 노드 우측 바로 옆 |
| 복귀 버스 (D: Exception→Normal) | 출발 수평 구간 **위 -13px**, X는 출구 노드 우측 바로 옆 |

---

## 📌 8. 조건 결과 출구 우선순위 (Conditional Outcome Exit Priority)

> **결과 라벨이 출구 위치를 결정하며, 이 규칙은 메인/하위 스코프 및 좌/우 컬럼 분류보다 우선한다.**

| 결과 라벨 | 출발 위치 | 경로 및 진입 |
|---|---|---|
| `OK`, `Logged`, `Assigned` 등 정상 결과 | 박스 하단 중앙 | 다음 박스 상단 중앙으로 하향 직진 |
| `True` | 박스 좌측 상단에서 2/3 지점 (`Y + 2H/3`) | 좌측 전용 고유 레인으로 우회 후, 아래에 있는 목적지 박스는 항상 상단 중앙으로 진입 |
| `False`, `Fail`, `Failed`, `Error` | 박스 우측 하단 (`Y + 2H/3`) | 우측 전용 고유 레인을 거쳐 목적지로 진입 |
| `Timeout` | 박스 우측 상단 (`Y + H/3`) | 우측 전용 고유 레인을 거쳐 목적지로 진입 |

- `True` 레인은 연결마다 좌측으로 분리한다.
- `True`의 목적지가 출발 박스보다 아래에 있으면, 목적지 위쪽에서 중앙 X로 정렬한 뒤 마지막 수직 세그먼트로 목적지 상단 중앙에 진입한다. 위 또는 같은 높이의 목적지는 좌측으로 진입한다.
- `False`/`Timeout`은 연결마다 우측 전용 버스 레인을 분리하며, 목적지 진입점은 Rule 9의 마지막 접근 방향 규칙을 따른다.
- 같은 출발 노드의 `False`와 `Timeout`은 서로 다른 높이에서 출발하므로 최초 수평 세그먼트와 라벨이 겹치지 않아야 한다.
- `TimerStopped`는 정상 결과로, `System Error`는 예외 결과로 분류한다.

## 📌 9. 목적지 진입 방향 규칙 (Destination Entry Direction)

- 좌측에서 수평으로 접근하는 연결선은 목적지 박스의 **좌측 중앙**으로 진입한다.
- 우측에서 수평으로 접근하는 연결선은 목적지 박스의 **우측 중앙**으로 진입한다.
- 위에서 수직으로 내려오는 연결선은 목적지 박스의 **상단 중앙**으로 진입한다.
- `True`, `False`, `Timeout`의 목적지가 출발 박스보다 아래에 있으면, 버스 레인이 목적지 위쪽에서 정렬한 후 마지막 수직 세그먼트로 진입한다. 좌측 레인은 상단의 좌측 절반 지점 (`X + W/4`), 우측 레인은 상단의 우측 절반 지점 (`X + 3W/4`)을 사용한다.
- 정상 결과라도 목적지가 출발 노드보다 위 또는 같은 높이에 있으면, 녹색 직진선을 그리지 않고 파란 외곽 복귀 레인을 사용한다.
- 연결선 화살표는 각 진입 방향을 향해야 한다.

## 📌 10. 노드 배치 순서 (Node Placement Order)

- 순환이 없는 연결 그래프는 결정적 위상 정렬로 배치한다.
- 모든 연결선의 출발 노드는 목적지보다 먼저 배치되어야 하며, 일반 흐름이 위쪽으로 되돌아가 다른 박스를 관통해서는 안 된다.
- 순환 연결만 외곽 복귀 레인을 사용한다.

## 📌 11. 주 흐름과 보조 흐름 열 분리 (Primary and Auxiliary Columns)

- 시작 노드부터 각 액션의 첫 정상 결과를 따라가는 경로를 **주 흐름**으로 정의하고 좌측 열에 연속 배치한다.
- 주 흐름에 속하지 않는 오류 처리, 독립 체인, 보조 체인은 우측 열에 배치한다. 같은 우측 열에서 상단-하단 연결선이 겹치지 않으면 서로 다른 스트림도 그 열을 재사용한다.
- 여러 스트림이 합류하는 노드는 입력 스트림 중 가장 우측 열에 배치한다.
- 서로 연결되지 않은 보조 스트림은 노드와 상단-하단 연결선이 겹치지 않도록 연속 배치할 수 있으면 같은 우측 열을 재사용한다.
- 각 액션의 첫 정상 결과는 현재 열의 주 진행으로 유지한다. 그 외 모든 결과 응답(`wait`, `Timeout`, `System Error`, 이벤트 응답 등)은 같은 열을 재사용하지 않고 다음 우측 열에서 별도 스트림을 시작한다.
- `TimerStarted`, `TimerExpired`, `TimerStopped`는 타이머 이벤트 스트림 안에서의 정상 진행 결과로 분류한다.
- 열 간격은 고정값이 아니다. 각 열의 예외/분기 연결 수에 따라 다음 열 앞에 필요한 전용 버스 레인 수만큼만 간격을 확보한다. 단순 스트림은 좁게 유지하고 분기가 많은 스트림만 넓힌다.
- 같은 열의 정상 연결이 중간 박스를 건너뛰면, 박스 아래로 수직선을 계속 그리지 않는다. 연결마다 전용 우측 우회 레인을 사용하고 목적지 바로 위에서 상단 진입점으로 내려온다.
- 같은 목적지로 여러 우회선이 들어오면, 레인 번호마다 목적지 위의 접근 높이도 분리하여 마지막 수평 세그먼트까지 겹치지 않게 한다.
- 좌측 주 흐름에서 우측 보조 흐름으로 넘어가는 정상 연결은 녹색 직교 분기선으로 그리며, 목적지 상단의 좌측 절반 지점으로 수직 진입한다.
- 같은 열에서 위에서 아래로 이어지는 정상 연결은 하단 중앙 출발, 상단 중앙 진입, 아래 방향 화살표와 선 우측 라벨을 사용한다.

---

## 📌 12. 공용 그래프 배치 및 무충돌 라우팅 (최신 규칙)

> 본 규칙은 메인창과 compound action을 여는 하단 서브창에 공통 적용하며, 앞선 절의 고정 열 좌표·고정 버스 레인 규칙보다 우선한다.

- **연결 맵 선행**: 모든 action의 outcome을 수집해 출발 노드·도착 노드·결과 라벨·고유 순번을 가진 전체 연결 맵을 먼저 구성한다.
- **결정적 트리·레벨**: 정상 결과를 우선해 결정적 spanning tree를 선택하고 위상 depth를 세로 레벨로 사용한다. 모든 일반 연결의 목적지는 출발 노드보다 아래 레벨에 배치한다.
- **트리 직선·최단 배치**: 다음 레벨의 트리 자식은 가능한 경우 부모 바로 아래 동일 $X$ 슬롯에 배치한다. 충돌 시 부모와 수평 거리가 가장 짧은 빈 슬롯을 쓰며, 독립 박스만 좌측 빈 슬롯으로 압축한다.
- **연결 밀도·빈 슬롯 배치**: 열 연결 밀도가 높은 흐름을 좌측에 우선 배치한다. 레벨마다 같은 행의 빈 좌측 슬롯을 재사용해 연결이 적은 우측 묶음을 왼쪽으로 붙인다.
- **경계 통과 수 기반 열 간격**: 열 간격은 실제로 그 열 경계를 통과하는 전용 레인 수만 반영한다. 통과선이 없는 경계는 최소 간격을 사용한다.
- **레벨 행 간격**: 기본 행 여백은 `30px`, 해당 레벨의 입·출력 연결에 대한 추가 여백은 연결 하나당 `6px`이다.
- **고유 포트와 안전 여백**: 같은 박스 면의 포트는 최소 `10px` 간격의 고유 슬롯을 사용한다. 연결선은 출발·도착을 제외한 모든 박스의 외곽 `10px` 안전 여백 안으로 들어가지 않는다.
- **최소 굴절 우선순위**: 안전 여백과 포트를 만족하는 경로 중 `직선(0회) → L자(1회) → 전용 레인 우회` 순으로 선택한다. 전용 레인은 가장 가까운 빈 열 간 레인을 우선 사용하고, 내부 후보가 막힌 경우만 외곽으로 확장한다.
- **같은 열 하향 직선**: 결과 유형·트리 간선 여부와 관계없이 같은 열의 하향 연결은 중간 박스가 없으면 수직 직선으로 그린다. 정상은 초록, 예외는 빨강을 유지한다.
- **병렬 결과 분리**: 같은 출발·도착의 병렬 결과는 출발·도착에서 대응되는 `10px` 포트 슬롯을 사용해 나란한 수직선으로 그린다. 라벨은 슬롯별로 중간 높이의 위·아래에 최소 `12px` 간격으로 교대 배치한다.
- **동일 레벨 접근선 분리**: 같은 레벨의 서로 다른 목적지로 들어가는 전용 레인 연결은 최종 수평 접근 $Y$ 좌표를 공유하지 않으며, 레벨 전체에서 `10px` 간격으로 배정한다.
- **렌더링 순서**: `연결 맵 → 트리/레벨 배치 → 포트 배정 → 최소 굴절 라우팅 → 렌더링` 순서를 유지한다.

---

### 📝 버전 기록
- **v1.0.0 (2026-08-24)**: 최초 제정 (사용자 지시사항 반영 및 영구 명세서 파일화)
- **v1.1.0 (2026-08-24)**: Rule 4, 5 추가 — 직교 라우팅 원칙, 버스 트랙 겹침 방지, 색상 명세
- **v1.2.0 (2026-08-24)**: Rule 6 추가 — 정상 라벨 우선 컬럼 분류 (`True`→직진 하향 보장); Rule 7 추가 — 라벨 비겹침 배치 규칙
- **v1.3.0 (2026-08-24)**: 겹침 제로화 강화 —
  - 그룹 컨테이너 박스 제거 (라벨-박스 겹침 원인 제거; 복합 액션 노드는 색상으로 구분)
  - L→R 버스 트랙 간격 확대: 20px → 30px (버스선 시각적 혼잡 해소)
  - Return 버스 트랙 간격 확대: 26px → 30px, 시작 X=840으로 이동
  - 라벨 위치 노드 경계 외부로 강제 이동: 수직선 +22px, 브릿지/복귀 라벨 노드 우측 +20px
- **v1.4.0 (2026-08-24)**: 조건 결과 출구 우선순위 및 교차 최소화 —
  - `OK`, `Logged`, `Assigned` 등 정상 결과는 항상 박스 **하단 중앙**에서 다음 박스 **상단 중앙**으로 아래 방향 직진한다.
  - `True`는 컬럼과 스코프에 관계없이 조건 박스의 **좌측 중앙**에서 출발하고, 좌측 전용 고유 레인을 통해 목적지 박스 **좌측**으로 진입한다.
  - `False`/`Fail`/`Failed`/`Error`는 조건 박스의 **우측 상단 (`Y + H/3`)**에서 출발한다.
  - `Timeout`은 조건 박스의 **우측 하단 (`Y + 2H/3`)**에서 출발한다.
  - 조건 결과 출구 규칙은 메인/하위 스코프와 좌/우 컬럼 판정보다 우선한다. 예외 연결의 목적지 진입 Y 좌표는 연결마다 `10px`씩 분리해 겹쳐 보이는 수평 구간을 줄인다.
- **v1.5.0 (2026-08-24)**: 목적지 진입 앵커를 마지막 연결선 방향에 맞춰 통일 — 좌측 접근은 좌측 중앙, 우측 접근은 우측 중앙, 상단 접근은 상단 중앙으로 진입한다.
- **v1.6.0 (2026-08-24)**: 아래 목적지로 향하는 `True` 연결은 상단 경계에서 수평으로 닿지 않고, 목적지 위쪽에서 수직으로 내려와 상단 중앙으로 진입한다.
- **v1.7.0 (2026-08-24)**: 아래 목적지로 향하는 `False`/`Timeout` 연결에도 상단 수직 진입 규칙을 적용한다. 수평 진입이 필요한 경우에만 실제 접근 방향에 맞춰 좌·우 중앙을 사용한다.
- **v1.8.0 (2026-08-24)**: `Timeout` 출발점을 우측 상단 `Y + H/3`으로, `False` 계열 출발점을 우측 하단 `Y + 2H/3`으로 교체했다. 아래 목적지의 상단 진입점은 좌측/우측 레인에 따라 각각 `X + W/4`와 `X + 3W/4`로 분리한다.
- **v1.9.0 (2026-08-24)**: 09.01 시나리오의 결과 라벨을 보완했다. `TimerStopped`는 정상 하향 흐름으로, `System Error`는 우측 예외 흐름으로 분류한다.
- **v1.10.0 (2026-08-24)**: DFS 배치에서 위쪽 목적지로 향하는 정상 결과를 역방향 연결로 판정한다. 박스와 라벨을 관통하는 녹색 상향선 대신 파란 외곽 복귀 레인으로 렌더링한다.
- **v1.11.0 (2026-08-24)**: DFS 선행 순서를 결정적 위상 정렬로 교체했다. 여러 오류 처리 체인이 합류하는 09.01 시나리오에서도 출발 노드가 목적지보다 먼저 배치되어 역방향 연결을 최소화한다.
- **v1.12.0 (2026-08-24)**: 시작점의 첫 정상 결과를 기준으로 주 흐름을 좌측 열에 고정하고, 그 외 보조 체인과 오류 처리를 우측 열로 분리했다. 주 흐름에서 보조 흐름으로의 정상 전이도 화살표와 라벨을 갖는 녹색 직교 분기선으로 렌더링한다.
- **v1.13.0 (2026-08-24)**: 보조 흐름을 단일 우측 열에 섞지 않고 스트림마다 다음 우측 열로 분리했다. 합류 노드는 가장 우측 입력 스트림의 열에 배치해 일반 연결선이 왼쪽으로 되돌아가는 것을 방지한다.
- **v1.14.0 (2026-08-24)**: 서로 연결되지 않은 보조 스트림은 연결 컴포넌트 단위로 연속 배치하고 같은 우측 열을 재사용한다. 세로 연결선이 겹치지 않을 때 불필요하게 새 열을 만들지 않는다.
- **v1.15.0 (2026-08-24)**: 이벤트 대기의 `wait` 결과는 타이머 처리용 독립 스트림으로 보고 다음 우측 열에 배치한다.
- **v1.16.0 (2026-08-24)**: 열 분리 기준을 특정 `wait` 라벨에서 결과 응답 일반 규칙으로 확장했다. 각 노드의 첫 정상 결과만 현재 열을 유지하고, 나머지 결과는 다음 우측 열의 별도 스트림으로 시작한다.
- **v1.16.1 (2026-08-24)**: `TimerStarted`와 `TimerExpired`를 타이머 스트림의 정상 결과로 추가해, 타이머 처리 후속 액션이 불필요하게 다시 새 열로 분리되지 않도록 했다.
- **v1.17.0 (2026-08-24)**: 열 간격을 결과 분기 수 기반의 가변 폭으로 변경했다. 각 열의 전용 버스 레인을 수용하면서 넓은 빈 공간을 만들지 않는다.
- **v1.18.0 (2026-08-25)**: 같은 열에서 중간 박스를 건너뛰는 정상 연결을 전용 우측 우회 레인으로 분리했다. 동일 방향 연결선의 중첩과 박스 아래를 지나는 수직선을 방지한다.
- **v1.19.0 (2026-08-25)**: 하단 서브창 전용 전체 연결 맵, 결정적 트리 배치, 10px 고유 포트, 박스 안전 여백 및 전용 직교 레인 규칙을 추가했다.
- **v1.20.0 (2026-08-25)**: 하단 서브창에서 단독 인접 트리 연결을 최단 수직 직선으로 우선 렌더링하고, 열 간격을 실제 연결 레인 수 기반의 가변 폭으로 제한했다.
- **v1.21.0 (2026-08-25)**: 하단 서브창의 전체 연결 맵으로 열별 연결 밀도를 계산해, 연결이 많은 열부터 좌측에 배치하도록 했다.
- **v1.22.0 (2026-08-25)**: 하단 서브창 레인 라우팅에 직선·단일 굴절 우선순위를 추가해, 장애물이 없는 인접 연결의 불필요한 우회를 제거했다.
- **v1.23.0-v1.31.1 (2026-08-25)**: 메인·하단 공용 연결 맵과 트리/레벨 배치, 가장 가까운 빈 레인, 합류·병렬 포트 및 라벨 분리, 레벨별 빈 슬롯 재사용, 같은 열 직선 보장, 경계 통과 수 기반 열 압축, 메인 분기의 하향 배치, 절반 행 간격 규칙을 최신 공통 규칙으로 통합했다.
