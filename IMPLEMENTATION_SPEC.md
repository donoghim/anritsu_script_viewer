# Anritsu STD Scenario Viewer - 현재 구현 명세서

## 목적

본 문서는 현재 Anritsu STD Scenario Viewer에 구현된 동작을 정의한다. 이 프로그램은 Anritsu XML 시나리오 파일(`.test`)을 위한 Windows 데스크톱 뷰어이며, 액션 흐름 시각화, 중첩 compound action 탐색, 선택한 액션의 XML 파라미터 조회 기능을 제공한다. 호환 구현은 아래 기능 및 UI 동작을 보존해야 한다.

## 실행 환경 계약

- Python 3.8 이상, PyQt6, Windows 데스크톱, Fusion 애플리케이션 스타일을 사용한다.
- 프로젝트 루트에 `.venv` 이름의 Python 가상환경을 생성해 의존성을 격리한다.
- 가상환경 생성 및 의존성 설치 명령은 다음과 같다.

	```cmd
	py -m venv .venv
	.venv\Scripts\activate
	python -m pip install --upgrade pip
	python -m pip install PyQt6
	```

- PowerShell에서는 `\.venv\Scripts\Activate.ps1`로 활성화한다. 실행 정책 오류가 나면 현재 세션에 한해 `Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass`를 먼저 실행한다.
- 가상환경을 활성화하지 않고 실행할 때는 `.venv\Scripts\python.exe app.py [scenario-file]`를 사용한다.
- 실행 명령: `python app.py [scenario-file]`.
- 첫 번째 명령행 인자가 존재하는 파일 경로일 때만 자동으로 로드하고, 그렇지 않으면 빈 뷰어를 연다.
- 제품명과 버전은 루트의 `version.py`에서 `APP_NAME`, `VERSION`으로 단일 관리한다. 창 제목은 `<APP_NAME> v<VERSION>` 형식이다. 초기 크기는 1300 x 850 픽셀이다.
- 파일 선택 필터: `*.test`, `*.xml`, 모든 파일.
- 파싱 또는 로드 실패는 모달 오류 대화상자로 표시한다. 로드 실패가 기존에 표시된 시나리오를 대체해서는 안 된다.

# 기능 명세

## 시나리오 파싱

XML 요소 및 속성 이름을 비교할 때 네임스페이스를 무시한다.

시나리오 객체는 선택한 파일 경로, 루트의 `fileVersion` 및 `rtdVersion`, 직접 하위 `catalog` 요소의 텍스트, 직접 하위 `procedureLibrary` 요소의 이름/버전 레코드, XML 순서의 루트 액션, 루트 액션 ID 맵을 가진다.

모든 `action`은 다음 필드를 가진 노드로 재귀 파싱한다.

| 필드 | 원본 |
| --- | --- |
| `id`, `name`, `description` | action 속성, 없으면 빈 문자열 |
| `type` | 이름이 `type`으로 끝나는 첫 번째 속성의 값 |
| `action_type` | `controlActionType`, 없으면 `procedureActionType` |
| `procedure_lib` | `procedureLibraryName` |
| `parameters` | XML 순서의 직접 하위 `parameter` 요소 |
| `outcomes` | XML 순서의 직접 하위 `outcome` 요소 |
| `child_actions` | 재귀 파싱한 직접 중첩 액션 |
| `child_id_map` | 직접 하위 ID와 액션 노드의 매핑 |
| `layout_info` | 선택적 `x`, `y`, `row` 정수 딕셔너리 |

각 outcome은 `id`, `followingActionId`, `name`, `terminatorId`를 보존한다. `followingActionId`가 없으면 `-1`로 처리하며, 그래프 간선을 만들지 않는다.

액션의 직접 자식 액션이 해당 액션의 하위 scope를 정의한다. 액션 아래의 `displayInformation/layoutInformation/a`를 읽어 `a/@id`를 현재 액션 또는 직접 자식과 매칭하고, `x`, `y`, `row`를 레이아웃 정보로 저장한다. 이 정보는 원본 레이아웃 모드에서만 사용한다.

## 파일 로드

1. 사용자가 `Open Scenario File`을 클릭하거나, 애플리케이션이 유효한 명령행 경로를 받는다.
2. 새 시나리오를 파싱한다.
3. 성공하면 헤더를 `File: <name> (<full path>)`로 설정하고, 인스펙터 파일명을 설정하며, 루트 액션을 렌더링하고, 선택 및 하위 scope 탐색을 초기화하고, 하위 패널을 숨기고, 상세 정보를 비운다.
4. 실패하면 `Failed to load scenario file:`과 오류 내용을 표시한다.

## 선택 및 Compound 탐색

- 액션을 한 번 클릭하면 해당 그래프 패널에서 선택하고, 같은 패널의 이전 선택을 해제하며, 해당 패널의 scope prefix를 사용해 파라미터 인스펙터에 노드를 표시한다.
- 선택된 노드는 3픽셀 파란 테두리를 사용한다. 선택 노드에서 나가는 경로는 원래 색상을 125% 밝게 한 4픽셀 선으로 표시한다.
- `Center Selected`는 메인 그래프를 현재 선택 노드의 중앙으로 이동하며, 선택이 없으면 아무 동작도 하지 않는다.
- 메인 그래프에서 선택한 노드의 ID와 scope prefix는 별도 선택 상태로 유지한다. `Show Detail`, `Use displayInformation Layout` 전환 또는 동일 scope의 재렌더링 후에도 해당 노드가 존재하면 선택 테두리와 outgoing 연결선 강조를 다시 적용하고, `Center Selected`의 대상으로 복원한다.
- 선택한 노드가 재렌더링된 scope에 없거나 새 파일을 성공적으로 로드한 경우에만 선택 상태를 명시적으로 해제한다. 재렌더링 중 `selected_item` 객체가 초기화되었다는 이유만으로 메인 선택 상태를 잃으면 안 된다.
- 직접 자식이 있는 액션을 더블 클릭하면 해당 액션의 하위 scope를 연다. 그 외 더블 클릭은 동작하지 않는다.
- 루트에서 열면 child stack을 `<node>, root:<node-id>`로 초기화한다.
- 하위 뷰에서 열면 `<node>, <parent-prefix>:<node-id>`를 stack에 추가한다.
- 하위 패널은 stack 마지막 노드의 직접 자식을 표시한다.
- depth 1에서는 `Close Child Scope`가 stack을 비우고 패널을 숨긴다. 더 깊은 단계에서는 `Back to Parent Scope`가 마지막 항목만 제거한다.
- 하위 패널을 처음 열 때 왼쪽의 두 패널을 동일하게 분할한다. 이후 깊이 이동에서는 사용자가 조절한 분할 위치를 유지한다.

## 파라미터 인스펙터

선택 전에는 단계/유형/설명이 `-`이고 트리와 전이 목록은 비어 있다. 선택하면 다음을 표시한다.

- 굵은 글꼴의 `Step: <scope-prefix>:<id> - <name>`.
- `Type: <node-type> (<action-type>)`.
- `Description: <description>` 또는 `Description: -`.
- 재귀형 2열 파라미터 트리 및 읽기 전용 전이 영역.

직접 하위 `parameter`마다 트리 항목을 재귀 생성한다. `parameter`의 키는 type 속성에서 첫 번째 `.` 앞의 텍스트이며, 없으면 `Parameter`를 사용한다. 다른 키는 네임스페이스를 제외한 요소 이름이다. type 이외의 XML 속성은 `@attribute` 자식으로 추가한다. 리프 텍스트는 값 열에 넣는다. 자식과 자체 텍스트를 모두 가진 요소는 `value` 자식을 추가한다. 자식이 둘 이상이고 자체 텍스트가 없으면 `(<count> values)`를 표시한다. 선택 후 전체 트리를 확장한다.

목적지가 `-1`이 아닌 outcome마다 `-> <scope-prefix>:<target-id> (<outcome-name>)`를 추가한다. 자식 액션이 있는 노드는 `[enter] -> <current-step-id>:0 (Start Sub-Scope)`도 추가한다. `Expand All`과 `Collapse All`은 전체 파라미터 트리에 적용한다.

## 그래프 의미

각 표시 scope는 직접 액션으로 구성된 독립적인 방향 그래프다. outcome 대상이 같은 scope의 다른 직접 액션으로 해석될 때만 간선을 추가한다. XML 소스 순서는 결정 규칙의 동률 해소 기준이다. 시작 노드는 type이 `START`이거나 ID가 `0`인 첫 액션이며, 없으면 첫 노드다. 순환 노드는 계속 표시한다.

정상 outcome 라벨은 공백 제거 및 대소문자 무시 기준으로 `ok`, `assigned`, `logged`, `displayed`, `true`, `response received`, `timerstarted`, `timerexpired`, `timerstopped`, `authentication response`, `security mode complete (emm)`, `security mode complete (rrc)`, 빈 문자열이다. 나머지는 비정상이다. `timeout`은 비정상/빨간색이지만 도달 가능한 후속 흐름이 가장 길면 주 흐름을 계속하는 데 사용할 수 있다.

## 간선 맵 무결성 및 처리 순서

그래프는 반드시 다음 순서로 처리한다. `간선 맵 구성 -> 노드 배치 -> 간선 라우팅 -> 렌더링 -> 검증`.

1. **간선 맵 구성**: scope의 각 직접 액션과 모든 직접 outcome을 한 번만 순회한다. `followingActionId`가 같은 scope의 직접 액션 ID와 일치하는 경우에만 간선 레코드를 만든다. 간선 레코드는 최소한 고유 순번, 출발 노드 ID, 도착 노드 ID, outcome ID, 원본 라벨, 정상/비정상 분류, XML outcome 순서를 가진다.
2. **단일 진실 원천**: 위 간선 맵만이 배치의 incoming/outgoing 계산, 주 흐름 및 tree parent 선택, route track 예약, 연결선·화살표·라벨 렌더링의 유일한 입력이다. 노드의 화면상 인접성, 같은 lane/level 여부, 이름, 색상, 선택 상태, 이전 렌더링 경로로 간선을 추론하거나 새 간선을 만들면 안 된다.
3. **배치와 간선의 분리**: 노드 배치는 간선 맵의 출발/도착 관계를 참조할 수 있으나 간선을 변경하지 않는다. 배치 결과의 좌표는 각 간선의 경로를 계산하는 데만 사용하며, 배치 이후 간선의 출발/도착 ID를 재결합하거나 교환하면 안 된다.
4. **라우팅 입력 검증**: 간선을 라우팅하기 직전에 출발 ID와 도착 ID가 현재 scope의 노드 맵에 모두 존재하고, 해당 간선이 원본 간선 맵에 존재하는지 확인한다. 하나라도 만족하지 않으면 선, 화살표, 라벨을 그리지 않는다.
5. **렌더링 개수 검증**: 화살표와 outcome 라벨은 반드시 하나의 유효 간선에 종속된다. 렌더링 완료 후 연결선 수와 화살표 수는 유효 간선 수와 같아야 한다. 빈 outcome 라벨은 텍스트만 생략하며 간선 자체는 유지한다.
6. **재렌더링 초기화**: scope를 다시 그릴 때 이전 scene의 연결선, 화살표, 라벨, route track 예약, 선택 연결선 참조를 모두 제거한 뒤 새 간선 맵으로 다시 구성한다. 이전 scene의 그래픽 항목이 새 scope에 남으면 안 된다.

이 규칙에 따라 노드 사이에 XML outcome으로 정의되지 않은 연결선, 화살표 또는 라벨이 표시되어서는 안 된다.

## 노드 입력/출력 포트 관리

노드의 경계는 단순한 연결선 좌표가 아니라, 라우팅 전에 관리해야 하는 명시적 입력/출력 포트 집합이다. 모든 유효 간선은 하나의 출발 포트와 하나의 도착 포트를 배정받은 뒤에만 경로를 계산할 수 있다.

### 포트 모델

각 노드는 다음 논리 포트를 가진다.

| 포트 집합 | 역할 | 기본 위치 |
| --- | --- | --- |
| 출력 포트 | 해당 노드에서 시작하는 outcome 간선 | 박스 하단 |
| 입력 포트 | 다른 노드에서 들어오는 outcome 간선 | 박스 상단 |

- 포트는 노드 ID, scope prefix, 면(`top` 또는 `bottom`), 슬롯 번호, 실제 X 좌표, 점유 간선 ID를 포함하는 레코드로 관리한다.
- 기본 슬롯은 박스 중심 X이다. 추가 슬롯은 중심에서 오른쪽 10픽셀, 왼쪽 10픽셀, 오른쪽 20픽셀, 왼쪽 20픽셀 순으로 배정한다.
- 각 노드의 입력 포트 슬롯과 출력 포트 슬롯은 별도로 관리한다. 동일 노드라도 입력과 출력은 같은 슬롯 번호를 사용하거나 동일 X 좌표에 있을 수 있지만, 같은 면의 동일 슬롯은 두 간선에 배정할 수 없다.
- 포트의 실제 X 좌표는 노드 테두리 내부에서만 유효하다. 슬롯 수가 박스 폭을 초과하면 해당 노드 주변 level 간격 또는 노드 폭을 늘려 최소 10픽셀 포트 간격을 유지한다.

### 포트 배정 및 경로 연결 규칙

1. 노드 배치 후, 모든 유효 간선에 대해 출발 노드의 미점유 하단 출력 포트와 도착 노드의 미점유 상단 입력 포트를 먼저 배정한다. 간선이 많은 노드도 포트 부족으로 인해 기존 포트를 공유하면 안 된다.
2. 각 간선은 출발 박스의 배정 출력 포트에서 정확히 시작하고, 도착 박스의 배정 입력 포트에서 정확히 끝나야 한다. 화살표 끝점은 도착 입력 포트와 일치해야 한다.
3. 출발 포트에서 첫 굴절점까지의 하향 수직 선분과 마지막 굴절점에서 도착 포트까지의 하향 수직 선분은, 다른 간선의 포트 이탈/진입 선분과 X 좌표 또는 Y 구간을 공유하면 안 된다.
4. 같은 노드의 여러 출력선은 박스 하단에서 즉시 분리되어야 한다. 같은 노드의 여러 입력선은 박스 상단 직전에서 이미 분리된 각 입력 포트로 수직 진입해야 한다. 박스 경계 밖에서 합쳐진 뒤 한 포트로 들어가는 형태는 허용하지 않는다.
5. 포트 배정은 XML 간선 순서로 결정적으로 수행한다. 이후 경로 충돌로 간선을 재라우팅할 때에도 이미 배정된 포트는 유지한다. 포트를 변경해야만 충돌을 제거할 수 있으면, 충돌한 간선의 미점유 대체 슬롯을 다시 배정하고 해당 노드의 모든 영향 간선을 재검증한다.
6. 직접 수직선은 출발 출력 포트 X와 도착 입력 포트 X가 같을 때만 허용한다. 둘이 다르면, 경로가 짧더라도 최소 한 번의 수평 이동을 포함하는 직교 경로를 사용한다.
7. 선택 상태, 라벨, 화살표, 확대/축소, 재렌더링은 포트 배정 결과를 변경하지 않는다. 같은 scope와 같은 간선 맵을 다시 렌더링하면 같은 포트 슬롯을 재현해야 한다.

렌더링 전 검증으로 모든 유효 간선에 출발 포트 1개와 도착 포트 1개가 배정되었는지, 같은 노드 면에서 포트 슬롯이 중복되지 않는지, 연결선의 시작/끝 좌표가 배정 포트와 일치하는지를 검사한다. 하나라도 실패한 간선은 그리지 말고, 대체 포트/경로를 다시 계산한다.

### 모든 입력/출력 포트 연결 완전성 검사

배치와 포트 배정이 끝난 뒤, 렌더링 전에 현재 scope의 모든 노드에 대해 포트 완전성을 검사한다.

1. 각 노드의 기대 입력 포트 수는 간선 맵에서 해당 노드를 도착지로 하는 유효 incoming 간선 수와 같아야 한다. 기대 출력 포트 수는 해당 노드를 출발지로 하는 유효 outgoing 간선 수와 같아야 한다.
2. 각 기대 입력/출력 간선은 정확히 하나의 포트 슬롯, 하나의 충돌 없는 연결 경로, 하나의 대상 화살표를 가져야 한다. 동일 간선의 포트/경로/화살표가 둘 이상인 경우도 오류다.
3. 노드별로 `기대 입력 수 = 배정 입력 포트 수 = 렌더링 예정 도착 연결선 수`와 `기대 출력 수 = 배정 출력 포트 수 = 렌더링 예정 출발 연결선 수`가 모두 성립해야 한다.
4. 입력 또는 출력 포트가 하나라도 누락되거나, 유효 간선이 포트 없이 남거나, 포트는 있으나 경로/화살표가 없거나, 경로는 있으나 반대편 포트가 없으면 현재 배치안은 무효다.
5. 무효 배치안은 부분 렌더링하면 안 된다. 누락 간선만 임시로 덧그리거나 기존 포트를 공유하는 방식도 허용하지 않는다.
6. 무효가 발생하면 충돌/누락 간선과 관련 노드를 기록한 뒤, `노드별 연결 트리 및 레인 시뮬레이션`의 **트리 기반 노드 배치** 단계부터 포트 배정, 레인 후보 생성, 시뮬레이션, 효율성 평가를 다시 수행한다. 재시도마다 이전 포트 배정과 선분 예약은 폐기한다.
7. 모든 노드의 기대 입력/출력 수가 배정·라우팅·렌더링 예정 수와 일치하고, 각 경로가 충돌 검사를 통과할 때만 scene 렌더링을 허용한다.

## 노드별 연결 트리 및 레인 시뮬레이션

그래프 배치는 단일 위상 정렬 결과로 종료하지 않는다. 유효 간선 맵을 바탕으로 노드별 연결 트리를 구성하고, 노드 배치와 포트별 레인을 시뮬레이션한 결과가 충분히 짧고 충돌이 없을 때만 렌더링한다. 처리 단위는 다음의 반복 파이프라인이다.

`간선 맵 -> 노드별 연결 트리 -> 노드 배치 -> 포트 배정 -> 포트별 레인 후보 -> 레인 시뮬레이션 -> 효율성 평가 -> 렌더링`

### 1. 노드별 연결 트리 구성

1. 간선 맵에서 각 노드의 incoming/outgoing 간선을 수집한다.
2. Start 노드는 주 연결 트리의 루트로 한다. Start에서 도달 가능한 노드는 주 트리 또는 그 분기 트리에 포함한다.
3. 현재 scope에서 유효한 입력 포트, 즉 incoming 간선이 하나도 없는 Start 이외의 노드는 독립 연결 트리의 루트로 판정한다. 독립 트리 루트는 주 트리와 동일 column에 섞지 않고 새로운 오른쪽 column에 배치한다.
4. incoming 간선이 있으나 아직 어느 트리에도 속하지 않은 노드는, XML 순서가 가장 앞선 유효 incoming 간선을 부모로 선택하여 해당 부모의 연결 트리에 포함한다. 정상 간선, 주 흐름 간선, XML 순서 순으로 parent 후보를 우선한다.
5. 순환 구성 요소는 외부에서 들어오는 간선이 있으면 그 간선의 트리에 포함하고, 외부 입력이 없으면 하나의 독립 연결 트리로 취급한다. 순환 내부의 대표 루트는 XML 순서가 가장 앞선 노드다.
6. 각 트리는 노드 목록, tree 간선, 비tree 간선, 루트 노드, 사용 level 범위, 사용 lane 범위를 명시적으로 관리한다. tree 간선은 하향 배치의 우선 기준이고, 비tree 간선은 전용 route track 후보를 사용한다.

### 2. 트리 기반 노드 배치

1. 주 트리의 가장 긴 Start-도달 경로를 lane zero의 연속된 아래 level에 배치한다.
2. 각 tree child는 부모보다 반드시 아래 level에 배치한다. 부모와 충돌하지 않는 경우 같은 column을 우선하며, 분기 child는 가까운 오른쪽 column을 사용한다.
3. 독립 트리 루트는 기존 트리의 사용 lane 범위와 겹치지 않는 가장 가까운 새 오른쪽 column에 배치한다. 독립 트리의 child도 아래 방향과 짧은 tree 간선을 우선해 배치한다.
4. 같은 tree의 노드를 다른 column으로 이동할 때는 해당 tree 간선의 수평 거리와 이후 비tree 간선의 예상 route 길이를 함께 계산한다. 단순히 빈 공간이 있다는 이유만으로 먼 column으로 이동하면 안 된다.

### 3. 포트별 레인 후보와 시뮬레이션

1. 배치된 모든 노드에 대해 입력/출력 포트를 배정한다.
2. 각 간선에 대해 배정 출력 포트, 배정 입력 포트, 직통 후보, 내부 lane 후보, 외곽 lane 후보를 생성한다.
3. 각 후보 경로를 실제 좌표로 시뮬레이션한다. 시뮬레이션은 박스 안전 여백, 포트 슬롯, 다른 간선의 예약 선분, 화살표 공간, 라벨 공간, 연결 방향을 검사한다.
4. 시뮬레이션 결과는 간선별 경로, 굴절 수, 맨해튼 길이, 사용 track, 점유 수평/수직 선분, 충돌 목록을 포함해야 한다.
5. 모든 간선을 시뮬레이션한 뒤, 동일 track의 겹침, 포트 근처 합류, 수평/수직 교차, 노드 관통, 반대 방향 우회, 불필요하게 긴 외곽 우회를 전체 그래프 기준으로 검사한다.

### 4. 효율성 평가 및 트리 재구성

다음 중 하나면 현재 배치/라우팅은 비효율로 판정한다.

- 충돌 없는 경로를 찾지 못한 유효 간선이 하나라도 있다.
- 하나의 노드 포트 주변에서 두 연결선이 박스 외부 선분을 공유하거나 겹친다.
- tree 간선이 같은 column의 아래 방향 직통 또는 1회 굴절 경로보다 긴 우회 경로를 사용한다.
- 독립 연결 트리가 주 트리 또는 다른 독립 트리의 빈 오른쪽 column보다 먼 column을 사용해 전체 맨해튼 길이를 늘린다.
- 노드 이동으로 충돌을 제거하거나 전체 경로 길이, 굴절 수, 외곽 lane 수를 줄일 수 있는데도 기존 배치를 유지한다.

비효율로 판정되면 단순히 충돌 간선의 route track만 바꾸지 않는다. 충돌/장거리 간선의 원인이 되는 트리 parent 선택, 독립 트리 루트 column, 분기 child lane, level을 다시 평가한 뒤 `노드별 연결 트리 구성` 단계부터 전체 파이프라인을 재실행한다. 새 후보는 이전 후보보다 다음 순서로 더 좋아야 채택한다: 충돌 간선 수, 총 외곽 lane 수, 총 굴절 수, 전체 맨해튼 길이. 주 흐름의 Start 순서와 lane zero 위치는 유지한다.

동일한 트리/배치 상태가 다시 나타나거나, 모든 유효 간선이 충돌 없이 라우팅되고 모든 노드의 입력/출력 포트 완전성 검사를 통과하며 더 나은 후보가 없으면 반복을 종료한다. 종료 후에만 scene에 노드, 연결선, 화살표, 라벨을 렌더링한다.

## 라우팅 실패 시 노드 재배치

노드 배치는 한 번 계산하고 끝나는 고정 단계가 아니다. 포트 배정과 레인 라우팅 결과가 충돌 없는 경로를 만들 수 없음을 보이면, 라우팅 결과를 기준으로 영향을 받는 노드의 lane/level/간격을 다시 계산해야 한다. 포트 또는 연결선만 억지로 이동해 문제를 숨기면 안 된다.

다음 중 하나라도 발생하면 재배치를 수행한다.

- 유효 간선에 서로 다른 출발/도착 포트를 배정할 수 없다.
- 배정된 포트 사이에 박스 안전 여백과 예약 선분을 모두 만족하는 route track이 없다.
- 동일 노드의 입출력선, 서로 다른 간선의 수평/수직 선분, 또는 화살표 직전 선분이 겹치거나 교차한다.
- 경로가 출발지 반대 방향의 외곽으로 우회하거나, 최소 경로 후보보다 불필요하게 많은 굴절을 가진다.
- 같은 노드 위 또는 아래의 포트 이탈/진입 구간이 10픽셀 간격으로 분리될 수 없다.

재배치 절차는 다음과 같다.

1. 충돌 간선과 그 출발/도착 노드, 해당 간선이 통과하는 lane 경계를 충돌 집합으로 기록한다.
2. 주 흐름 lane과 이미 확정된 비충돌 노드의 상대 순서는 유지한다. 충돌 집합의 보조 대상 노드는 가장 가까운 오른쪽 빈 lane 또는 새 오른쪽 lane으로 옮긴다.
3. 같은 level의 포트 혼잡이 원인이면 대상 노드를 출발 노드보다 아래의 다음 빈 level로 내린다. 이때 연결된 후속 노드도 방향성이 유지되도록 함께 내린다.
4. 두 lane 사이의 route track 부족이 원인이면 해당 lane 경계의 간격을 10픽셀 단위로 늘려 필요한 독립 track 수와 10픽셀 안전 여백을 확보한다.
5. 수정된 노드 배치 전체에 대해 포트 배정, 선분 예약, 최소 경로 선택을 처음부터 다시 수행한다. 이전 포트 배정과 route track 예약은 재사용하지 않는다.
6. 모든 유효 간선이 독립 포트와 충돌 없는 경로를 얻을 때까지 반복한다. 재배치 전보다 선분 충돌 수가 줄어들지 않는 배치는 채택하지 않는다.

재배치 후에도 충돌이 남으면 그래프 폭 또는 높이를 확장해야 한다. 노드, 포트, 라벨, 화살표, 연결선을 겹쳐 표시하거나 일부 간선을 생략하는 방식으로 공간 부족을 해결하면 안 된다.

## 자동 그래프 배치

루트와 하위 scope 모두 다음 전체 간선 맵 알고리즘을 사용한다.

1. incoming 및 outgoing 간선 맵을 구성한다.
2. 비순환 노드를 위상 정렬하되 시작 노드를 우선하고, 순환만으로 구성된 노드는 XML 순서로 뒤에 추가한다.
3. Start 노드에서 도달 가능한 모든 노드와 간선을 먼저 표시한다. Start에서 정상 또는 `timeout` outgoing 간선을 선택해 주 흐름을 만들고, 가장 긴 도달 가능 후속 흐름을 우선한다. 동률이면 정상 간선, 이후 outcome 순서를 우선한다.
4. 대상마다 이전 level의 tree parent를 하나 선택한다. 주 흐름 간선, 정상 간선, XML 소스/outcome 순서 순으로 우선한다.
5. tree parent로 level을 재계산한다. 주 흐름 level은 0부터 연속되도록 강제한다. 주 흐름 밖의 대상은 자신에게 연결되는 모든 출발 노드보다 아래에 둔다.

Start에서 도달 가능한 간선은 순환 간선이 아닌 한 항상 출발 노드보다 아래의 level로 연결한다. 따라서 연결선은 위로 올라가지 않으며, 수직 직통 또는 아래 방향의 직교 경로를 사용한다. Start에서 도달 가능한 노드를 단지 수평 공간 절약을 위해 출발 노드보다 위 또는 같은 level에 배치하면 안 된다.

Start에서 도달할 수 없는 노드는 독립 흐름으로 처리한다. 각 독립 흐름의 첫 노드는 기존 Start-도달 흐름과 충돌하지 않는 가장 가까운 새 오른쪽 컬럼에 배치한다. 같은 독립 흐름의 후속 노드는 그 흐름의 유효 간선이 아래 방향 및 최소 길이가 되도록, 첫 노드 아래의 같은 컬럼을 우선 사용한다. 분기 또는 합류 때문에 같은 컬럼을 사용할 수 없을 때만 인접한 오른쪽 컬럼을 추가한다.

초기 배치 뒤에는 모든 Start-미도달 흐름에 대해 연결선 길이를 다시 평가한다. 각 흐름의 노드 level/lane을 이동했을 때 안전 여백, 포트, 다른 간선 예약을 만족하면서 해당 흐름의 전체 맨해튼 길이를 줄일 수 있으면 그 배치를 채택한다. 이 최적화는 Start-도달 주 흐름의 순서와 lane zero 위치를 변경하면 안 된다.

좌표 규칙:

- 노드 박스: 270 x 44 픽셀, lane zero X=100, 첫 행 Y=40.
- 주 흐름 노드는 lane zero에 둔다.
- 선택된 정상 tree child는 주 흐름 밖 부모의 lane을 이어가며, 분기는 가장 가까운 비점유 lane을 선택한다.
- 보조 lane은 전체 간선 밀도 순으로 재배치한다. level 범위가 겹치지 않을 때만 화면 lane을 재사용한다.
- 기본 lane 간격은 40픽셀이며, 해당 lane 경계를 넘는 경로의 제곱근 그룹이 증가할 때마다 20픽셀씩 확장한다.
- 기본 세로 간격은 30픽셀이며, 해당 행 경계에서 나가거나 접근하는 비직선 간선 하나당 10픽셀을 추가한다.
- 연결된 노드는 배정 lane을 유지한다. 고립된 노드만 빈 왼쪽 슬롯을 재사용할 수 있다.

`Use displayInformation Layout`을 켜면 레이아웃 정보가 있는 노드는 최소 X/Y가 X=100/Y=40이 되도록 정규화한 원본 좌표를 사용한다. 노드는 Y, X, XML 순서로 배치한다. 사각형이 앞서 배치한 사각형과 20픽셀 여유 영역까지 겹치면, 겹치지 않을 때까지 오른쪽으로 290픽셀씩 이동한다. 레이아웃 정보가 없는 노드는 자동 좌표를 유지한다.

## 그래프 라우팅

모든 링크는 직교 형태이며, 박스 뒤에 그리는 2픽셀 선과 대상에 위치한 8픽셀 채움 삼각형 화살표를 사용한다.

대상이 아래에 있고, 출발/대상이 자동 배치에서 같은 열에 있거나 원본 레이아웃에서 호환되는 수평 범위에 있으며, 10픽셀 여유 통로에 제3의 박스가 없으면 수직 직통 경로를 사용한다. 출발 박스 하단과 대상 박스 상단을 연결한다. 병렬 직통 링크는 10픽셀 간격의 교대 포트를 사용한다.

그 외 경로는 고유한 출발 하단 포트, 12픽셀 하향 이탈과 level별 10픽셀 offset, 수직 route track, 대상 위쪽의 수평 접근선, 대상 상단 포트를 사용한다. 포트 offset은 중앙, 오른쪽 10, 왼쪽 10, 오른쪽 20, 왼쪽 20 순이다. 열 사이에 10픽셀 간격으로 배치된 내부 track을 우선하며, 막히면 대상 방향의 외곽 track을 사용한다. track은 종점 이외 박스에서 10픽셀을 유지하고, 수직 구간이 10픽셀 이내로 겹치는 track을 재사용하지 않는다. 대상 접근선은 대상 위 12픽셀에서 시작하고, 접근 충돌이 있을 때만 5픽셀 단위로 위로 이동한다.

원본 레이아웃 모드에서는 유효한 방향성 track 중 Manhattan 길이가 가장 짧은 것을 선택한다. 출발 중심에서 아래로 내려가는 통로가 비어 있으면 대상 바로 위에서 한 번만 꺾는 짧은 경로를 사용한다.

정상 간선은 초록 RGB(40,150,40), 모든 비정상 간선은 빨강 RGB(210,30,30)을 사용한다. 비어 있지 않은 outcome 라벨은 경로 색상으로 표시하며, 직통 경로의 중간 옆 또는 출발 측 경로 구간 위에 둔다.

### 최소 연결선 선택

각 유효 간선은 노드 배치와 입력/출력 포트 배정이 완료된 뒤 독립적으로 경로 후보를 평가해 하나의 경로만 선택한다. 후보는 박스 안전 여백, 배정 포트, 예약된 연결선 구간, 출발/도착 방향 규칙을 모두 만족해야 한다. 후보 우선순위는 다음과 같다.

1. 장애물이 없는 같은 열 하향 직통 수직선.
2. 장애물이 없는 하향 1회 굴절 경로.
3. 두 노드 사이의 내부 track을 사용하는 최단 직교 경로.
4. 대상 방향의 외곽 track을 사용하는 최단 직교 경로.

같은 우선순위의 후보가 둘 이상이면 전체 맨해튼 길이 $L = \sum |x_{i+1}-x_i| + |y_{i+1}-y_i|$가 가장 짧은 경로를 선택하고, 다시 동률이면 XML 간선 순서와 가까운 track을 우선한다. 라우팅을 위해 필요하지 않은 굴절, 같은 선분의 왕복, 목적지와 반대 방향으로의 불필요한 외곽 우회는 허용하지 않는다. 다른 간선과 충돌해 선택할 수 없는 후보는 경로 개수나 길이가 더 짧더라도 제외한다.

최소 경로 후보가 하나도 없으면 해당 간선을 강제로 그리지 않는다. 먼저 `라우팅 실패 시 노드 재배치` 절차를 수행하고, 재배치된 좌표를 기준으로 후보를 다시 평가한다.

### 하위 Scope 연결선 비겹침

하위 scope를 포함한 모든 그래프에서, 서로 다른 두 연결선의 선분이 동일한 좌표 구간에 겹쳐 보이면 안 된다. 공통 출발 또는 도착 노드가 있더라도 박스 경계에서만 만날 수 있으며, 박스 외부의 수평 또는 수직 선분을 공유해서는 안 된다.

1. 라우팅은 간선의 XML 순서로 결정적으로 처리한다. 경로를 확정한 간선은 수평/수직 선분의 좌표와 시작/종료 구간을 예약한다.
2. 다음 간선은 예약된 선분과 같은 X 또는 Y 좌표에서 구간이 겹치는 경로를 사용할 수 없다. 단, 서로 다른 구간 사이 간격이 10픽셀 이상이면 같은 track을 재사용할 수 있다.
3. 하나의 출발 노드에서 나가는 모든 연결선은 서로 다른 하단 포트를 사용한다. 하나의 도착 노드로 들어오는 모든 연결선도 서로 다른 상단 포트를 사용한다. 포트 간격은 최소 10픽셀이다.
4. 직통 수직 연결선도 전역 선분 예약 검사 대상이다. 이미 예약된 수직 선분과 Y 구간이 겹치거나, 동일한 접근 높이에서 수평선이 교차/중첩되면 직통 경로를 포기하고 전용 route track으로 우회한다.
5. 수평 이탈선, 대상 접근선, 화살표 직전 선분까지 모두 예약 대상으로 취급한다. 연결선은 다른 연결선 위를 덮어 그리거나 동일 구간을 반대 방향으로 공유해서는 안 된다.
6. 같은 level에 여러 대상 접근선이 필요한 경우, 대상별 접근 높이를 최소 5픽셀씩 분리하고, 수평 구간이 겹치면 추가로 5픽셀씩 분리한다.

검증 기준은 하위 scope의 밀집 분기에서 초록/빨강 연결선이 한 줄처럼 합쳐 보이지 않고, 각 연결선을 시작 포트부터 화살표까지 독립적으로 따라갈 수 있는 것이다.

## 노드 상세 텍스트

모든 박스의 첫 줄은 `<marker><scope-prefix>:<id>: <name>`이다. 직접 자식 액션이 있을 때만 marker `[+] `를 붙인다. `Show Detail`이 켜져 있으면, 사용 가능한 항목을 ` | `로 연결한 두 번째 줄을 추가한다.

- parameter 하위의 `DisplayName`에서 `DisplayName: <value>`.
- `Timeout` 하위의 `value-`, `variable-`, `valuemodify-`에서 `Timeout: <value>`.
- `duration` 텍스트에서 `Duration: <value>`.
- `CONDITIONAL_BRANCH`는 `ConditionalExpression`에서 `Condition: <value>`를 먼저 추가한다. 없으면 `ControlVariable` 자식을 `<name>=<value>`로 조합하되 `value`, `value-`는 제외한다.

# UI 명세

## 전체 레이아웃

중앙 레이아웃은 4픽셀 margin과 spacing을 사용한다. 상단 바와 수평 splitter로 구성한다. 왼쪽은 메인 그래프와 선택적 하위 그래프가 있는 수직 splitter이며 최소 폭은 550픽셀이다. 오른쪽은 최소 폭 350픽셀의 파라미터 인스펙터다. splitter 자식은 접힐 수 없어야 한다.

초기 수평 크기는 780과 480픽셀이며 왼쪽:오른쪽 stretch 비율은 60:40이다. 하위 패널 표시 상태가 바뀔 때, 전체 폭이 700픽셀을 넘는 경우에만 이 비율을 복원한다.

사용자가 수평 splitter(메인 그래프 영역:인스펙터 영역) 또는 수직 splitter(메인 그래프:하위 그래프)를 조절한 뒤에는 해당 비율을 사용자 설정으로 저장하고 유지한다. 이후 노드 선택, `Show Detail`/`Use displayInformation Layout` 전환, 동일 파일의 scope 재렌더링, 하위 scope depth 이동은 사용자 설정 비율을 변경하거나 초기값으로 되돌리면 안 된다. 수직 splitter는 하위 패널을 닫아도 마지막 사용자 비율을 보존하며, 다음에 하위 패널을 다시 열 때 복원한다. 수평 splitter 비율은 새 파일을 열어도 유지한다. 창 크기가 바뀔 때에는 저장된 비율을 기준으로 두 패널 크기를 재계산하되, 각 패널의 최소 폭/높이를 우선한다.

## 상단 바와 그래프 패널

- 상단 바 margin: 4, 2, 4, 2 픽셀.
- `Open Scenario File`: 굵은 9포인트 Malgun Gothic, 최소 높이 30픽셀.
- 늘어나는 파일 라벨의 초기 문구는 `No file loaded`이며, 색상은 `#555555`, 왼쪽 padding은 10픽셀이다.
- 각 그래프 패널은 2픽셀 margin/spacing 및 굵은 9포인트 Malgun Gothic `Scope: <prefix>` 라벨을 사용한다.
- 메인 헤더에는 `Center Selected`, 하위 헤더에는 close/back 버튼이 있다.
- 캔버스는 안티앨리어싱 `QGraphicsView`를 사용한다. 손 끌기로 이동하고, 일반 휠로 스크롤하며, Ctrl-휠로 포인터 중심 기준 매 단계 1.15배 확대/축소한다.
- 메인 및 하위 그래프의 초기 확대 배율은 각각 100%여야 한다. 파일 로드, 원본 레이아웃 전환, 하위 scope 열기/이동 시 `fitInView` 또는 전체 그래프 자동 맞춤 축소를 적용하지 않는다.
- 두 그래프 패널의 확대 배율은 독립적으로 관리한다. 한 패널의 Ctrl-휠 확대/축소 또는 재렌더링이 다른 패널의 변환 행렬을 바꾸면 안 된다.
- 새 scope를 표시하면 확대 배율은 100%로 초기화하고, 시작 노드가 보이는 위치를 초기 스크롤 위치로 설정한다. 그래프 전체가 화면보다 넓거나 높으면 사용자는 스크롤 또는 손 끌기로 탐색한다.

## 액션 박스 모양

박스는 270 x 44픽셀, 중앙 정렬된 굵은 8포인트 Malgun Gothic RGB(25,25,25) 텍스트와 2픽셀 테두리를 사용한다. 아래 표에서 처음 일치하는 스타일을 적용한다. `START` action은 이름, compound 여부, 자식 액션 보유 여부, terminator 명칭, 기타 모든 분류 조건보다 높은 최우선 순위로 START 스타일을 적용한다. `CompoundTerminator`를 포함해 이름에 `terminator`가 있는 노드와 action type이 `END`인 노드는 START가 아닌 경우 compound 여부보다 terminator 스타일을 우선한다.

| 분류 | 채우기 | 테두리 |
| --- | --- | --- |
| `START` | RGB(225,228,232) | RGB(130,130,130) |
| 이름에 `terminator` 포함 또는 type `END` | RGB(218,254,218) | RGB(60,170,60) |
| Compound 또는 자식 보유 | RGB(254,218,218) | RGB(210,60,60) |
| 이름이 정확히 `LogMessage` | White | RGB(60,120,210) |
| `CONDITIONAL_BRANCH` | RGB(224,204,239) | RGB(130,70,170) |
| 이름에 wait/timer/event/mmi 포함 | RGB(255,240,150) | RGB(190,150,20) |
| 기본값 | RGB(218,234,254) | RGB(60,120,210) |

선택 테두리는 RGB(30,90,255), 3픽셀이다.

노드 레이블은 1행의 액션 식별 정보와 선택적 2행의 상세 정보로 구성한다. 1행 `<marker><scope-prefix>:<id>: <name>`은 항상 완전하게 표시되어야 하며, 2행이 길더라도 축소, 생략, 가림, 겹침, 세로 밀어내기가 발생하면 안 된다. 상세 정보는 1행의 가용 폭을 침범하지 않는 독립 영역에 표시한다. 2행 텍스트가 박스 폭을 넘으면, 줄바꿈보다 먼저 박스의 가용 폭 안에서 시작 부분부터 표시 가능한 최대 길이까지 보여 주고, 남은 부분만 말줄임표로 표시한다. 이 처리에서 1행의 글꼴, 위치, 가시 폭은 변경하면 안 된다. 상세 정보를 여러 줄로 표시하는 구현은 허용되며, 이 경우 박스 높이를 늘려 1행과 2행의 텍스트/테두리/연결선이 서로 겹치지 않도록 해야 한다. `Show Detail`을 끄면 2행 영역을 제거하고 1행을 박스 중앙에 유지한다.

## 인스펙터 모양

인스펙터는 6픽셀 margin/spacing을 사용한다. `Step Details`는 내부 margin 6픽셀, spacing 2픽셀이며 `version.py`의 `VERSION`을 사용한 `Version: v<VERSION>`, 파일, 단계, 유형, 설명, `Parameters:`를 표시한다. 도구 모음 순서는 `Expand All`, `Collapse All`, `Use displayInformation Layout`, `Show Detail`이며 spacing은 4픽셀이다. 마지막 두 버튼은 checkable이고, 원본 레이아웃은 해제 상태, 상세 표시는 선택 상태로 시작한다.

툴팁은 각각 `Render action boxes using the scenario displayInformation coordinates`, `Show or hide second-line box details`이다. 파라미터 트리 헤더는 `Key`, `Value`이며 Key는 내용에 맞추고 Value는 늘어난다. 교차 행 색상을 사용한다. `Transitions / Conditions`에는 최대 높이 90픽셀의 읽기 전용 편집기가 있다.

두 checkable 옵션 중 하나를 전환하면 루트와 표시 중인 하위 그래프를 즉시 다시 구성한다. XML 원본 데이터나 하위 scope 탐색 depth는 변경하지 않는다.

## 수용 확인 항목

1. 빈 실행: 하위 패널 없음, `No file loaded`, 빈 선택 상태.
2. 유효 파일 로드: 루트 액션 박스와 전체 경로 라벨 표시.
3. 노드 클릭: 파란 테두리, 강조된 outgoing 링크, 채워진 파라미터 트리와 전이 목록.
4. compound 더블 클릭: 하위 scope가 열리고 중첩 back 탐색 가능.
5. `Show Detail`: 두 패널 모두 제목 전용과 제목+상세 텍스트가 전환됨.
6. 원본 레이아웃: 원본 좌표 정규화 및 충돌 보정.
7. Ctrl-휠 확대/축소, 일반 휠 스크롤, 끌기 이동 동작.
8. 링크는 직교, 화살표, 올바른 색상을 사용하고 통로가 비었으면 수직 직통 경로를 사용.